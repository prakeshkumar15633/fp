{
    "version":2,
    "builds": [
      { "src": "*.js", "use": "@vercel/node" }
    ],
    "routes": [
        {
          "src": "/admin-api/(.*)",
          "dest": "/admin-api"
        }
    ]
  }